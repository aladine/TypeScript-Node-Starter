extends ../layout

block content
  .page-header
    h3 Restaurant profile

    form.form-horizontal(action='restaurant/detail', method='POST')
      input(type='hidden', name='_csrf', value=_csrf)
      .form-group